var Classe = require("classe");
var Renderer = require("./renderer");

/**
 * @class Container - A wrapper around a PIXI.Container, so we can have co-ordinates relative to the game.
 */
var Container = Classe({
});

module.exports = Container;
