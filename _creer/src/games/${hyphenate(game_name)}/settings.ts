import * as Settings from "src/viseur/settings";
Settings.BaseSetting.newIndex = 0; // reset to zero for these new settings

/** The settings for the ${game_name} game. */
export const GameSettings = {
${merge("    // ", "settings", "    // put settings here, look at previous games for examples", help=False)}
};
